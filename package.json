{
  "name": "timer-server",
  "version": "1.0.0",
  "description": "Timer application server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "docker-build": "docker build -t timer-app .",
    "docker-start": "docker-compose up",
    "docker-stop": "docker-compose down",
    "security-check": "npm audit",
    "update-deps": "npm update"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
